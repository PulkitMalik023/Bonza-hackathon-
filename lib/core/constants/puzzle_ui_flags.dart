/// Shows decorative badge on coin add button until purchase flow exists.
const bool kShowCoinAddBadge = true;

/// Shows decorative badge on bottom hint button.
const bool kShowHintBadge = true;

/// Shows decorative badge on Rewards bottom-nav tab until rewards flow exists.
const bool kShowRewardsNavBadge = true;

/// Coins awarded when a puzzle is completed.
const int kPuzzleCompletionCoinReward = 100;

/// Starting coin balance for new players.
const int kInitialCoinBalance = 75;
